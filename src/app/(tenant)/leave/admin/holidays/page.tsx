/**
 * Holidays config — /leave/admin/holidays.
 * Year selector, Add Holiday / Import from CSV. Table: date, day, name, optional, actions.
 */
export default function HolidaysAdminPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Holidays</h1>
      {/* TODO: Year selector, Add Holiday dropdown, table, holiday-form-drawer, holiday-import-dialog */}
    </div>
  );
}
